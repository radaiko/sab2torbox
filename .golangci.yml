version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    rules:
      # Test code routinely ignores errors from setup/teardown helpers.
      - path: _test\.go
        linters:
          - errcheck
